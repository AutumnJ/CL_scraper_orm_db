# frozen_string_literal: true

source "https://rubygems.org"

#git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem 'nokogiri'
gem 'require_all' #make loading all the files easy
gem 'sqlite3'
gem 'pry'
gem 'rake'
gem 'activerecord', :require => "active_record" #add this to use active record -- library is active_record, so must use lodash when requiring